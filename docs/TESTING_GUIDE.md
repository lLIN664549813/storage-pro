# Storage Pro 测试指南

## 📋 迭代 2 功能测试

### 1. 实时监控系统测试

#### 1.1 启动监控
1. 打开 Chrome DevTools
2. 切换到 Storage Pro 面板
3. 点击工具栏中的 **▶️ 播放按钮**
4. 确认按钮变为 **⏹️ 停止按钮**，且显示为红色
5. 查看 Footer 区域，应显示"监控中"状态

#### 1.2 测试变更检测
在浏览器控制台（Console）中执行以下命令：

```javascript
// 测试添加数据
localStorage.setItem('test_key_1', 'test_value_1')

// 测试修改数据
localStorage.setItem('test_key_1', 'modified_value')

// 测试删除数据
localStorage.removeItem('test_key_1')

// 测试批量添加
for (let i = 0; i < 5; i++) {
  localStorage.setItem(`test_${i}`, `value_${i}`)
}
```

#### 1.3 查看变更日志
1. 点击工具栏中的 **📋 日志按钮**
2. 应该看到刚才的所有变更记录
3. 每条记录显示：
   - 时间戳
   - 操作类型（设置/删除/清空）
   - 键名
   - 旧值和新值

#### 1.4 测试高亮显示
1. 在控制台执行：`localStorage.setItem('highlight_test', 'value')`
2. 在左侧 Key 列表中，`highlight_test` 应该：
   - 显示绿色背景
   - 显示"最近变更"标签
   - 3秒后高亮消失

#### 1.5 停止监控
1. 点击 **⏹️ 停止按钮**
2. 按钮变回 **▶️ 播放按钮**
3. Footer 区域不再显示监控状态
4. 在控制台修改 storage，不应再记录新的变更

---

### 2. 统计面板测试

#### 2.1 打开统计面板
1. 点击工具栏中的 **📊 统计按钮**
2. 应该展开统计面板，显示：
   - 总项数
   - 总大小
   - 最大项
   - 配额使用百分比

#### 2.2 测试统计数据
在控制台执行：

```javascript
// 添加不同类型的数据
localStorage.setItem('string_data', 'Hello World')
localStorage.setItem('number_data', '12345')
localStorage.setItem('json_data', JSON.stringify({ name: 'Test', age: 25 }))
localStorage.setItem('boolean_data', 'true')
localStorage.setItem('null_data', 'null')

// 添加大数据项
localStorage.setItem('large_data', 'x'.repeat(10000))
```

刷新页面后，统计面板应显示：
- 类型分布更新
- 最大项显示为 `large_data`
- 配额使用百分比增加

---

### 3. Monaco Editor 测试

#### 3.1 切换到 Monaco 编辑器
1. 选择任意一个 JSON 类型的项
2. 点击"编辑"按钮
3. 在编辑器顶部，点击 **Monaco 编辑器** 按钮
4. 应该看到 Monaco Editor 加载

#### 3.2 测试编辑功能
1. 在 Monaco Editor 中修改 JSON 内容
2. 观察语法高亮是否正常
3. 尝试输入错误的 JSON 语法，应该显示红色波浪线
4. 按 **Ctrl+S** (Windows) 或 **Cmd+S** (Mac) 保存
5. 确认数据已保存

#### 3.3 测试自动补全
1. 在 JSON 对象中输入 `"`
2. 应该自动补全为 `""`
3. 输入 `{`，应该自动补全为 `{}`

---

### 4. 变更日志功能测试

#### 4.1 查看日志详情
1. 启动监控
2. 执行一些 storage 操作
3. 点击 **📋 日志按钮**
4. 查看日志列表，每条记录应包含：
   - 时间
   - 操作类型（带颜色标记）
   - 键名
   - 旧值/新值

#### 4.2 测试过滤功能
1. 在日志对话框中，选择"操作类型"下拉框
2. 选择"设置"，应该只显示 set 操作
3. 在搜索框中输入关键字，应该过滤匹配的记录

#### 4.3 导出日志
1. 点击"导出日志"按钮
2. 应该下载一个 JSON 文件
3. 文件包含所有变更记录和监控状态

#### 4.4 清空日志
1. 点击"清空日志"按钮
2. 确认提示
3. 日志列表应该清空

---

## 🐛 已知问题与修复

### 已修复问题 ✅
1. **初始值监控问题** - 已修复
   - 问题：启动监控时会将所有现有数据记录为"新增"
   - 修复：启动时先创建快照，只监控之后的变化
   
2. **时间显示不更新** - 已修复
   - 问题：监控运行时长显示为固定值，不实时更新
   - 修复：使用定时器每秒更新一次运行时长

### 当前已知问题
- **轮询延迟**: 使用500ms轮询，可能有最多500ms的延迟
- **性能影响**: 频繁轮询可能对性能有轻微影响
- **仅支持 localStorage**: 当前版本仅监控 localStorage，sessionStorage 支持待添加

### Monaco Editor
- **加载时间**: 首次加载 Monaco Editor 需要1-2秒
- **内存占用**: Monaco Editor 会增加约20MB内存占用

---

## 💡 测试技巧

### 快速生成测试数据
```javascript
// 生成100个测试项
for (let i = 0; i < 100; i++) {
  localStorage.setItem(`test_item_${i}`, JSON.stringify({
    id: i,
    name: `Item ${i}`,
    timestamp: Date.now()
  }))
}
```

### 清空测试数据
```javascript
// 删除所有 test_ 开头的项
Object.keys(localStorage)
  .filter(key => key.startsWith('test_'))
  .forEach(key => localStorage.removeItem(key))
```

### 模拟大数据
```javascript
// 创建接近配额限制的数据
const largeData = 'x'.repeat(1024 * 1024) // 1MB
localStorage.setItem('large_test', largeData)
```

---

## 📊 性能基准

### 监控系统
- 轮询间隔: 500ms
- 最大日志数: 1000条
- 内存占用: < 5MB

### Monaco Editor
- 加载时间: 1-2秒
- 内存占用: ~20MB
- 支持文件大小: < 1MB

---

## 🔗 相关文档

- [需求文档](../.kiro/specs/storage-pro-enhancement/requirements.md)
- [迭代计划](../.kiro/specs/storage-pro-enhancement/ITERATION_PLAN.md)
- [进度跟踪](../.kiro/specs/storage-pro-enhancement/PROGRESS.md)

---

**文档版本**: v1.0  
**创建日期**: 2025-12-31  
**最后更新**: 2025-12-31
